{
  "name": "socket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  	// here we are informing heroku that server should be started using following command
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  // here we are informing heroku that which version of node should be used
  "engines": {
    "node": "8.11.3"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "socket.io": "^1.4.8"
  }
}
